# DataFlow examples

Standalone, copy-pasteable starter projects.

| Example | What it shows |
|---|---|
| [`node-ws-server/`](./node-ws-server) | Reference WebSocket server in ~250 lines of Node — emits financial / IoT / ecommerce rows in the exact wire format the DataFlow WS adapter expects. Point any React / Vue / Svelte client at `ws://localhost:8080?scenario=financial` and it just works. |

More examples land here as we go. Pull requests welcome.
